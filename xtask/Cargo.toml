[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
snapdog = { path = "../snapdog", default-features = false }
knx-prod = { git = "https://github.com/metaneutrons/knx-rs", branch = "main" }
