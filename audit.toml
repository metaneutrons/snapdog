# Advisories that cannot be fixed — waiting on upstream crate updates.
# Review periodically and remove once upstream releases a fix.

[advisories]
ignore = [
    # rsa: Marvin Attack timing sidechannel — no patch available.
    # Pulled by: shairplay, librespot-core
    "RUSTSEC-2023-0071",

    # rustls-webpki: CRL distribution point matching, URI name constraints, wildcard name constraints.
    # Pulled by: rumqttc 0.25 → rustls 0.22 → rustls-webpki 0.102.
    # Fix requires rumqttc to upgrade to rustls 0.23+.
    "RUSTSEC-2026-0049",
    "RUSTSEC-2026-0098",
    "RUSTSEC-2026-0099",
    "RUSTSEC-2026-0104",

    # rustls-pemfile: unmaintained — pulled by rumqttc 0.25.
    "RUSTSEC-2025-0134",

    # rand 0.8: unsound with custom logger + thread_rng reseeding.
    # Pulled by: shairplay, librespot (via rsa → num-bigint-dig).
    # Fix requires upstream to bump to rand 0.9.3+.
    "RUSTSEC-2026-0097",
]
